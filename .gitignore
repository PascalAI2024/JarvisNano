# Upstream esp-claw clone (bootstrapped by scripts/bootstrap.sh)
esp-claw/

# Build artifacts
build/
.build_logs/
.build_tools/
.gradle/
.ccache/
managed_components/
dependencies.lock
sdkconfig
sdkconfig.old
sdkconfig.ci
__pycache__/
*.py[cod]

# IDE / OS noise
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Local agent / tooling state — Claude Code, Cursor, etc.
# (may contain absolute paths and personal config)
.claude/
.cursor/
.antigravitycli/

# Local diagnostic scratch (firmware logs, support emails, etc.)
.private/

# Secrets / local overrides — NEVER commit
.env
.env.*
*.local
.local/
secrets.yaml
*.key
*.token
*.pem
*.p12
credentials*

# mascot-eng local image-gen venv
.venv-mascot/
firmware/mascot/__pycache__/
